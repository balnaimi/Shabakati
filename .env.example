# ============================================
# Server Configuration
# ============================================

# Server Port
# In Production, both frontend and API run on the same port (3001)
# In Development, frontend runs on 5173 and API on 3001
PORT=3001

# ============================================
# CORS Configuration
# ============================================

# Allowed CORS Origins (comma-separated, no spaces after commas)
# This setting controls which domains are allowed to make requests to your API
#
# IMPORTANT: Different ports for different scenarios:
#
# Port 5173: Vite development server (DEVELOPMENT ONLY)
#   - Frontend runs on this port when using "npm run dev"
#   - NEVER use this port in Production
#
# Port 3000: Optional alternative for development (OPTIONAL)
#   - Some tools/projects use port 3000 as default
#   - Can be omitted if you don't use it
#   - NEVER use this port in Production
#
# Port 3001: Backend API server (DEVELOPMENT & PRODUCTION)
#   - API always runs on this port
#   - In Production, frontend is also served from this port
#   - This is the ONLY port you need in Production
#
# === DEVELOPMENT Configuration ===
# When NODE_ENV=development, use:
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:3001
# Explanation: Frontend (5173) needs to call API (3001), so both are needed
#
# === PRODUCTION Configuration ===
# When NODE_ENV=production, use ONLY your actual server domain with port 3001:
# ALLOWED_ORIGINS=http://YOUR_DOMAIN:3001,https://YOUR_DOMAIN:3001
# Or using IP: ALLOWED_ORIGINS=http://YOUR_SERVER_IP:3001
# Explanation: In Production, frontend and API are on the same port (3001), 
#              so you only need to allow your production domain
#
# Replace YOUR_DOMAIN with your actual domain or IP address
# Example for Production: http://example.com:3001,https://example.com
# Example for Development: http://localhost:5173,http://localhost:3001
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://YOUR_DOMAIN:3001

# ============================================
# Base URL Configuration
# ============================================

# Base URL for the server (used for constructing API links, without trailing slash)
# In Production: Replace YOUR_DOMAIN with your actual server domain or IP address
# Example: http://example.com:3001 or http://192.168.1.100:3001
# In Development: Leave empty or use http://localhost:3001
# If not set, defaults to http://localhost:{PORT}
BASE_URL=http://YOUR_DOMAIN:3001

# ============================================
# Environment Mode
# ============================================

# Environment mode: development or production
# When using Docker Compose, set to production
# This determines whether the server serves the built frontend or just the API info
#
# Development (NODE_ENV=development):
#   - Frontend runs separately on port 5173 (Vite dev server)
#   - API runs on port 3001
#   - Use localhost ports in ALLOWED_ORIGINS
#
# Production (NODE_ENV=production):
#   - Everything runs on port 3001 (same server)
#   - Frontend files are served from /dist folder
#   - Use only your production domain with port 3001 in ALLOWED_ORIGINS
NODE_ENV=production

# ============================================
# Rate Limiting (Optional)
# ============================================

# Time window for rate limiting in milliseconds (default: 900000 = 15 minutes)
# RATE_LIMIT_WINDOW_MS=900000

# Maximum number of requests per time window per IP (default: 100)
# RATE_LIMIT_MAX_REQUESTS=100
