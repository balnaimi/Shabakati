# لوحة تحكم الشبكة 🌐

لوحة تحكم حديثة لإدارة ومتابعة المضيفين في شبكتك مع قاعدة بيانات SQLite.

## المميزات

- ➕ إضافة مضيفين جديدين بسهولة
- 📋 عرض المضيفين كبطاقات جميلة
- 💾 حفظ في قاعدة بيانات SQLite
- 🔄 تغيير حالة المضيف (متصل/غير متصل)
- 🗑️ حذف المضيفين
- 🔗 روابط URL قابلة للنقر
- 📱 تصميم متجاوب يعمل على جميع الأجهزة
- 🌙 Dark Mode

## التثبيت والتشغيل

### 1. تثبيت مكتبات Frontend

```bash
cd /home/mrn/project
npm install
```

### 2. تثبيت مكتبات Backend

```bash
cd /home/mrn/project/server
npm install
```

### 3. تشغيل الخادم (Backend)

```bash
cd /home/mrn/project/server
npm start
# أو للتطوير مع auto-reload:
npm run dev
```

الخادم سيعمل على: `http://localhost:3001`

### 4. تشغيل Frontend

في terminal جديد:

```bash
cd /home/mrn/project
npm run dev
```

بعد ذلك افتح المتصفح على العنوان الذي سيظهر (عادة `http://localhost:5173`)

## قاعدة البيانات

- **النوع**: SQLite
- **الموقع**: `server/network.db`
- **الجدول**: `hosts`
  - `id` (INTEGER PRIMARY KEY)
  - `name` (TEXT)
  - `ip` (TEXT)
  - `description` (TEXT)
  - `url` (TEXT)
  - `status` (TEXT)
  - `created_at` (TEXT)

## API Endpoints

- `GET /api/hosts` - الحصول على جميع المضيفين
- `GET /api/hosts/:id` - الحصول على مضيف واحد
- `POST /api/hosts` - إضافة مضيف جديد
- `PUT /api/hosts/:id` - تحديث مضيف
- `DELETE /api/hosts/:id` - حذف مضيف
- `PATCH /api/hosts/:id/toggle-status` - تغيير حالة المضيف

## الاستخدام

1. املأ النموذج على اليمين لإضافة مضيف جديد
2. أدخل اسم المضيف وعنوان IP (مطلوب)
3. أضف وصف اختياري ورابط URL واختر الحالة
4. اضغط "إضافة المضيف"
5. ستظهر البطاقة في القسم الرئيسي
6. يمكنك تغيير الحالة أو حذف المضيف من البطاقة
7. يمكنك الضغط على رابط URL لفتحه في صفحة جديدة

## التقنيات المستخدمة

### Frontend
- React
- Vite
- CSS3 (Dark Mode)

### Backend
- Node.js
- Express
- SQLite (better-sqlite3)
- CORS

## ملاحظات

- تأكد من تشغيل الخادم قبل فتح Frontend
- قاعدة البيانات تُنشأ تلقائياً عند أول تشغيل للخادم
- البيانات محفوظة في ملف `server/network.db`

## ⚠️ متطلبات DNS

**مهم**: للحصول على أسماء المضيفين (hostnames) عند مسح الشبكة، يجب أن يكون النظام (الخادم) يستخدم DNS server يحتوي على **PTR records** للأجهزة.

- البرنامج يستخدم DNS servers الافتراضية للنظام
- تأكد من أن DNS server في النظام يحتوي على PTR records للأجهزة
- إذا لم تظهر أسماء المضيفين، تحقق من إعدادات DNS في النظام
