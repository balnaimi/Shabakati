# لوحة تحكم الشبكة 🌐

**For English version, see [README_EN.md](README_EN.md)**

لوحة تحكم حديثة لإدارة ومتابعة الأجهزة في شبكتك مع قاعدة بيانات SQLite.

## المميزات

- 📡 إدارة الشبكات (إضافة، تعديل، حذف)
- 🔍 فحص الشبكة لاكتشاف الأجهزة تلقائياً
- 📊 عرض بصري للشبكة (مربعات ملونة تمثل حالة كل IP)
- 📈 إحصائيات لكل شبكة (متصل، غير متصل، إجمالي)
- ➕ إضافة وإدارة الأجهزة
- 🔍 البحث والفلترة حسب الاسم، IP، الحالة، والوسوم
- 🏷️ إدارة الوسوم مع ألوان مخصصة
- 💾 حفظ في قاعدة بيانات SQLite

---

## المتطلبات

- **Node.js 24 LTS** أو أحدث
- **Docker و Docker Compose** (للتشغيل في Production)

---

## 🚀 التشغيل في Production

### الطريقة الأولى: باستخدام Docker (موصى به)

```bash
# 1. Clone المشروع
git clone https://github.com/balnaimi/Shabakati.git
cd Shabakati

# 2. تشغيل البرنامج
docker-compose up -d

# 3. فتح المتصفح
# http://localhost:3001
```

**إدارة Container:**
```bash
docker-compose stop      # إيقاف
docker-compose start     # تشغيل
docker-compose down      # إيقاف وحذف
docker-compose logs -f   # عرض السجلات
```

### الطريقة الثانية: بدون Docker

```bash
# 1. تثبيت المتطلبات
npm install
cd server && npm install && cd ..

# 2. بناء Frontend
npm run build

# 3. تشغيل Backend
cd server
npm start
```

**ملاحظة:** يمكنك استخدام PM2 لإدارة العملية الخلفية:
```bash
npm install -g pm2
cd server
pm2 start server.js --name network-dashboard
pm2 save
pm2 startup
```

---

## 💻 التشغيل في وضع التطوير

### الطريقة السريعة: استخدام السكريبتات

**Linux / macOS:**
```bash
./scripts/install-nodejs.sh  # تثبيت Node.js والمتطلبات
./scripts/dev.sh             # تشغيل البرنامج
```

**Windows:**
```powershell
.\scripts\install-nodejs.ps1  # تثبيت Node.js والمتطلبات (كمسؤول)
.\scripts\dev.ps1             # تشغيل البرنامج
```

بعد التشغيل، البرنامج سيكون متاحاً على:
- **الواجهة:** http://localhost:5173
- **API:** http://localhost:3001/api

### الطريقة اليدوية

**Terminal 1 - Backend:**
```bash
cd server
npm install
npm run dev
```

**Terminal 2 - Frontend:**
```bash
npm install
npm run dev
```

ثم افتح المتصفح على: http://localhost:5173

---

## 📝 ملاحظات

- قاعدة البيانات تُنشأ تلقائياً في `server/network.db`
- في Production، الخادم يخدم ملفات Frontend والـ API من نفس المنفذ (3001)
- في Development، Frontend يعمل على المنفذ 5173 والخادم على 3001

---

## 📄 الترخيص

راجع ملف `LICENSE` للتفاصيل.
