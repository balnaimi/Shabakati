# لوحة تحكم الشبكة 🌐

لوحة تحكم حديثة لإدارة ومتابعة الأجهزة في شبكتك مع قاعدة بيانات SQLite.

## المميزات

- ➕ إضافة أجهزة جديدة بسهولة
- 📋 عرض الأجهزة في جدول منظم مع إمكانية الترتيب والفلترة
- 💾 حفظ في قاعدة بيانات SQLite
- 🔄 التحقق من حالة الأجهزة (متصل/غير متصل)
- 🗑️ حذف الأجهزة
- 🔗 روابط URL قابلة للنقر
- 🌙 Dark Mode / Light Mode
- 🏷️ إدارة الوسوم (Tags) مع ألوان مخصصة
- 🔍 مسح الشبكة تلقائياً للعثور على الأجهزة
- 📊 إحصائيات مفصلة (إجمالي، متصلة، غير متصلة)
- 🔄 التحقق التلقائي من حالة الأجهزة بشكل دوري
- 🔄 التحقق من جميع الحالات مع مؤشر التقدم
- 📅 عرض تاريخ الإضافة وتاريخ آخر تحقق
- 🎨 تصميم ثابت ومستقر مخصص لأجهزة الكمبيوتر والشاشات الكبيرة

---

## 🚀 التثبيت والتشغيل

### المتطلبات

- Node.js (الإصدار 20 أو أحدث - موصى به: v22.x.x LTS)
- npm أو yarn

### الخطوات السريعة

#### 1️⃣ تثبيت مكتبات Frontend (الواجهة الأمامية)

افتح terminal وانتقل إلى مجلد المشروع:

```bash
cd /home/mrn/Shabakati
npm install
```

#### 2️⃣ تثبيت مكتبات Backend (الخادم)

في نفس terminal أو terminal جديد:

```bash
cd /home/mrn/Shabakati/server
npm install
```

#### 3️⃣ تشغيل الخادم (Backend)

في terminal:

```bash
cd /home/mrn/Shabakati/server
npm start
```

أو للتطوير مع auto-reload (يُعيد التشغيل تلقائياً عند التعديل):

```bash
cd /home/mrn/Shabakati/server
npm run dev
```

✅ **الخادم سيعمل على:** `http://localhost:3001`

**⚠️ اترك هذا Terminal مفتوحاً!**

#### 4️⃣ تشغيل Frontend (الواجهة)

افتح **terminal جديد** (لا تغلق terminal الخادم):

```bash
cd /home/mrn/Shabakati
npm run dev
```

✅ **الواجهة ستعمل على:** `http://localhost:5173`

#### 5️⃣ فتح البرنامج

افتح المتصفح واذهب إلى:
```
http://localhost:5173
```

---

## 📋 ملخص الأوامر

### Terminal 1 (الخادم):
```bash
cd /home/mrn/Shabakati/server
npm install  # فقط أول مرة
npm start    # أو npm run dev للتطوير
```

### Terminal 2 (الواجهة):
```bash
cd /home/mrn/Shabakati
npm install  # فقط أول مرة
npm run dev
```

---

## ⚠️ ملاحظات مهمة

1. **يجب تشغيل الخادم أولاً** قبل تشغيل الواجهة
2. **لا تغلق terminal الخادم** أثناء استخدام البرنامج
3. **قاعدة البيانات** تُنشأ تلقائياً في `server/network.db` عند أول تشغيل
4. إذا ظهرت أخطاء، تأكد من:
   - تثبيت جميع المكتبات (`npm install` في كلا المجلدين)
   - تشغيل الخادم قبل الواجهة
   - عدم استخدام المنفذ 3001 أو 5173 في برنامج آخر

---

## 🔧 حل المشاكل الشائعة

### خطأ: "Port 3001 already in use"
**الحل:** أغلق البرنامج الذي يستخدم المنفذ 3001 أو غير المنفذ في `server/server.js`

### خطأ: "Cannot find module"
**الحل:** شغل `npm install` في المجلد الذي يظهر فيه الخطأ

### خطأ: "EADDRINUSE" على المنفذ 5173
**الحل:** أغلق terminal الواجهة وأعد تشغيله

### خطأ: "Network request failed"
**الحل:** تأكد من أن الخادم يعمل على `http://localhost:3001`

---

## 📁 هيكل المشروع

```
Shabakati/
├── src/                    # كود الواجهة الأمامية (React)
│   ├── components/         # المكونات المشتركة
│   │   ├── ErrorBoundary.jsx
│   │   └── ThemeToggle.jsx
│   ├── pages/              # الصفحات الرئيسية
│   │   ├── HostsList.jsx  # الصفحة الرئيسية - عرض الأجهزة
│   │   ├── AddHost.jsx    # إضافة جهاز جديد
│   │   ├── NetworkScan.jsx # مسح الشبكة
│   │   └── TagsManagement.jsx # إدارة الوسوم
│   ├── hooks/              # React Hooks
│   │   └── useTheme.js
│   ├── utils/              # دوال مساعدة
│   │   └── api.js
│   ├── constants.js        # الثوابت
│   ├── App.jsx
│   └── main.jsx
├── server/                 # كود الخادم (Node.js)
│   ├── server.js           # ملف الخادم الرئيسي
│   ├── database.js         # إدارة قاعدة البيانات
│   ├── hostChecker.js      # التحقق من حالة الأجهزة
│   ├── networkScanner.js   # مسح الشبكة
│   ├── package.json
│   └── network.db          # ملف قاعدة البيانات (يُنشأ تلقائياً)
├── dist/                   # ملفات البناء (بعد npm run build)
├── public/                 # الملفات الثابتة
│   └── favicon.svg
├── package.json            # مكتبات الواجهة
├── vite.config.js          # إعدادات Vite
└── README.md
```

---

## 💾 قاعدة البيانات

- **النوع**: SQLite
- **الموقع**: `server/network.db`
- **الجداول**:
  - `hosts` - الأجهزة
  - `tags` - الوسوم
  - `host_tags` - ربط الوسوم بالأجهزة
  - `host_status_history` - تاريخ حالات الأجهزة

### جدول hosts:
- `id` (INTEGER PRIMARY KEY)
- `name` (TEXT) - اسم الجهاز
- `ip` (TEXT) - عنوان IP
- `description` (TEXT) - الوصف
- `url` (TEXT) - رابط URL
- `status` (TEXT) - الحالة (online/offline)
- `created_at` (TEXT) - تاريخ الإضافة
- `last_checked` (TEXT) - تاريخ آخر تحقق
- `ping_latency` (REAL) - زمن الاستجابة
- `packet_loss` (REAL) - فقدان الحزم
- `uptime_percentage` (REAL) - نسبة الوقت المتصل

### الفهارس (Indexes):
تم إضافة فهارس لتحسين الأداء:
- `idx_hosts_ip` - على عمود IP
- `idx_hosts_status` - على عمود status
- `idx_hosts_created_at` - على created_at
- `idx_host_tags_host_id` - على host_id في host_tags
- `idx_host_tags_tag_id` - على tag_id في host_tags
- `idx_status_history_host_id` - على host_id في host_status_history
- `idx_status_history_checked_at` - على (host_id, checked_at)

---

## 🔌 API Endpoints

### الأجهزة (Hosts)
- `GET /api/hosts` - الحصول على جميع الأجهزة (مع دعم pagination)
- `GET /api/hosts/:id` - الحصول على جهاز واحد
- `POST /api/hosts` - إضافة جهاز جديد
- `PUT /api/hosts/:id` - تحديث جهاز
- `DELETE /api/hosts/:id` - حذف جهاز
- `POST /api/hosts/:id/check-status` - التحقق من حالة الجهاز
- `GET /api/hosts/:id/history` - الحصول على تاريخ الحالات

### الوسوم (Tags)
- `GET /api/tags` - الحصول على جميع الوسوم
- `GET /api/tags/:id` - الحصول على وسم واحد
- `POST /api/tags` - إضافة وسم جديد
- `PUT /api/tags/:id` - تحديث وسم
- `DELETE /api/tags/:id` - حذف وسم

### مسح الشبكة
- `POST /api/network/scan` - مسح نطاق IP للعثور على الأجهزة النشطة

### استيراد/تصدير
- `GET /api/export` - تصدير البيانات (JSON)
- `POST /api/import` - استيراد البيانات (JSON)

---

## 📖 الاستخدام

### إضافة جهاز جديد
1. اضغط على زر "إضافة جهاز جديد"
2. أدخل اسم الجهاز وعنوان IP (مطلوب)
3. أضف وصف اختياري ورابط URL
4. اختر الوسوم (اختياري)
5. اضغط "إضافة الجهاز"
6. سيتم التحقق تلقائياً من حالة الجهاز

### عرض وإدارة الأجهزة
1. ستظهر جميع الأجهزة في جدول منظم
2. يمكنك البحث والفلترة حسب الحالة أو الوسوم
3. يمكنك ترتيب الجدول حسب الاسم، IP، الحالة، تاريخ الإضافة، أو تاريخ آخر تحقق
4. يمكنك عرض/تعديل/حذف أي جهاز
5. يمكنك التحقق من حالة جهاز واحد أو جميع الأجهزة

### مسح الشبكة
1. اضغط على "مسح الشبكة"
2. أدخل نطاق الشبكة (مثال: `192.168.30.1-254` أو `192.168.30.0/24`)
3. اضغط "بدء المسح"
4. اختر الأجهزة المراد إضافتها (الأجهزة المضافة مسبقاً ستظهر كمضاف مسبقاً)
5. اضغط "إضافة المحدد"

### إدارة الوسوم
1. اضغط على "إدارة الوسوم"
2. أضف وسوم جديدة مع لون مخصص أو عدّل الموجودة
3. يمكنك ربط الوسوم بالأجهزة عند الإضافة أو التعديل

### التحقق التلقائي
1. اضغط على زر "التحقق التلقائي" لتفعيل/إيقاف التحقق الدوري
2. سيتم التحقق من حالة جميع الأجهزة كل 5 دقائق (قابل للتعديل)

---

## 🛠️ التقنيات المستخدمة

### Frontend
- **React 18** - مكتبة واجهة المستخدم
- **Vite** - أداة البناء السريعة
- **React Router** - التوجيه بين الصفحات
- **Lucide React** - الأيقونات
- **React Toastify** - الإشعارات
- **CSS3** - التصميم مع Dark/Light Mode
- **Tajawal Font** - خط عربي جميل

### Backend
- **Node.js** - بيئة التشغيل
- **Express** - إطار عمل الخادم
- **SQLite (better-sqlite3)** - قاعدة البيانات
- **CORS** - السماح بالطلبات من مصادر مختلفة
- **ping** - التحقق من حالة الأجهزة

---

## ⚡ تحسينات الأداء

تم تطبيق عدة تحسينات لزيادة أداء التطبيق:

### قاعدة البيانات
- ✅ فهارس على الأعمدة المستخدمة بكثرة لتحسين سرعة الاستعلامات بنسبة 50-90%
- ✅ جلب الوسوم بشكل batch بدلاً من loop (تقليل من N+1 إلى 2 استعلامات)
- ✅ دعم pagination في API لتقليل حجم البيانات المرسلة

### Network Scanner
- ✅ فحص المنافذ بالتوازي بدلاً من التسلسلي (تقليل وقت المسح بنسبة 60-70%)
- ✅ زيادة batch size إلى 200
- ✅ تحسين timeout للمسح الأسرع

### React Frontend
- ✅ استخدام `useCallback` و `useMemo` لتقليل إعادة الـ render
- ✅ استخدام `Intl.Collator` للترتيب السريع والدقيق للنصوص العربية
- ✅ تحسين خوارزمية ترتيب IP addresses

---

## ⚠️ متطلبات DNS

**مهم**: للحصول على أسماء الأجهزة (hostnames) عند مسح الشبكة، يجب أن يكون النظام (الخادم) يستخدم DNS server يحتوي على **PTR records** للأجهزة.

- البرنامج يستخدم DNS servers الافتراضية للنظام
- تأكد من أن DNS server في النظام يحتوي على PTR records للأجهزة
- إذا لم تظهر أسماء الأجهزة، تحقق من إعدادات DNS في النظام

---

## 📝 ملاحظات إضافية

- قاعدة البيانات تُنشأ تلقائياً عند أول تشغيل للخادم
- البيانات محفوظة في ملف `server/network.db`
- يمكنك نسخ ملف `network.db` للنسخ الاحتياطي
- جميع الخطوط تستخدم خط **Tajawal** العربي
- التصميم ثابت ومستقر مخصص لأجهزة الكمبيوتر والشاشات الكبيرة
- البرنامج يدعم RTL (من اليمين لليسار) بشكل كامل

---

## 📄 الترخيص

راجع ملف `LICENSE` للتفاصيل.

---

**تاريخ التحديث:** 2024
