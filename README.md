# لوحة تحكم الشبكة 🌐

لوحة تحكم حديثة لإدارة ومتابعة المضيفين في شبكتك مع قاعدة بيانات SQLite.

## المميزات

- ➕ إضافة مضيفين جديدين بسهولة
- 📋 عرض المضيفين في جدول منظم
- 💾 حفظ في قاعدة بيانات SQLite
- 🔄 تغيير حالة المضيف (متصل/غير متصل)
- 🗑️ حذف المضيفين
- 🔗 روابط URL قابلة للنقر
- 📱 تصميم متجاوب يعمل على جميع الأجهزة
- 🌙 Dark Mode / Light Mode
- 🏷️ إدارة الوسوم (Tags)
- 🔍 مسح الشبكة تلقائياً
- 📊 إحصائيات مفصلة
- 🔄 التحقق التلقائي من حالة المضيفين

---

## 🚀 التثبيت والتشغيل

### الخطوات السريعة

#### 1️⃣ تثبيت مكتبات Frontend (الواجهة الأمامية)

افتح terminal وانتقل إلى مجلد المشروع:

```bash
cd /home/mrn/Shabakati
npm install
```

#### 2️⃣ تثبيت مكتبات Backend (الخادم)

في نفس terminal أو terminal جديد:

```bash
cd /home/mrn/Shabakati/server
npm install
```

#### 3️⃣ تشغيل الخادم (Backend)

في terminal:

```bash
cd /home/mrn/Shabakati/server
npm start
```

أو للتطوير مع auto-reload (يُعيد التشغيل تلقائياً عند التعديل):

```bash
cd /home/mrn/Shabakati/server
npm run dev
```

✅ **الخادم سيعمل على:** `http://localhost:3001`

**⚠️ اترك هذا Terminal مفتوحاً!**

#### 4️⃣ تشغيل Frontend (الواجهة)

افتح **terminal جديد** (لا تغلق terminal الخادم):

```bash
cd /home/mrn/Shabakati
npm run dev
```

✅ **الواجهة ستعمل على:** `http://localhost:5173`

#### 5️⃣ فتح البرنامج

افتح المتصفح واذهب إلى:
```
http://localhost:5173
```

---

## 📋 ملخص الأوامر

### Terminal 1 (الخادم):
```bash
cd /home/mrn/Shabakati/server
npm install  # فقط أول مرة
npm start    # أو npm run dev للتطوير
```

### Terminal 2 (الواجهة):
```bash
cd /home/mrn/Shabakati
npm install  # فقط أول مرة
npm run dev
```

---

## ⚠️ ملاحظات مهمة

1. **يجب تشغيل الخادم أولاً** قبل تشغيل الواجهة
2. **لا تغلق terminal الخادم** أثناء استخدام البرنامج
3. **قاعدة البيانات** تُنشأ تلقائياً في `server/network.db` عند أول تشغيل
4. إذا ظهرت أخطاء، تأكد من:
   - تثبيت جميع المكتبات (`npm install` في كلا المجلدين)
   - تشغيل الخادم قبل الواجهة
   - عدم استخدام المنفذ 3001 أو 5173 في برنامج آخر

---

## 🔧 حل المشاكل الشائعة

### خطأ: "Port 3001 already in use"
**الحل:** أغلق البرنامج الذي يستخدم المنفذ 3001 أو غير المنفذ في `server/server.js`

### خطأ: "Cannot find module"
**الحل:** شغل `npm install` في المجلد الذي يظهر فيه الخطأ

### خطأ: "EADDRINUSE" على المنفذ 5173
**الحل:** أغلق terminal الواجهة وأعد تشغيله

### خطأ: "Network request failed"
**الحل:** تأكد من أن الخادم يعمل على `http://localhost:3001`

---

## 📁 هيكل المشروع

```
Shabakati/
├── src/                    # كود الواجهة الأمامية (React)
│   ├── components/         # المكونات المشتركة
│   ├── pages/              # الصفحات الرئيسية
│   ├── hooks/              # React Hooks
│   ├── utils/              # دوال مساعدة
│   └── constants.js        # الثوابت
├── server/                 # كود الخادم (Node.js)
│   ├── server.js           # ملف الخادم الرئيسي
│   ├── database.js          # إدارة قاعدة البيانات
│   ├── hostChecker.js      # التحقق من حالة المضيفين
│   ├── networkScanner.js   # مسح الشبكة
│   └── network.db          # ملف قاعدة البيانات (يُنشأ تلقائياً)
├── dist/                   # ملفات البناء (بعد npm run build)
├── package.json             # مكتبات الواجهة
└── server/package.json      # مكتبات الخادم
```

---

## 💾 قاعدة البيانات

- **النوع**: SQLite
- **الموقع**: `server/network.db`
- **الجداول**:
  - `hosts` - المضيفين
  - `tags` - الوسوم
  - `host_tags` - ربط الوسوم بالمضيفين
  - `host_status_history` - تاريخ حالات المضيفين

### جدول hosts:
- `id` (INTEGER PRIMARY KEY)
- `name` (TEXT)
- `ip` (TEXT)
- `description` (TEXT)
- `url` (TEXT)
- `status` (TEXT)
- `created_at` (TEXT)
- `last_checked` (TEXT)
- `ping_latency` (REAL)
- `packet_loss` (REAL)
- `uptime_percentage` (REAL)

---

## 🔌 API Endpoints

### المضيفين (Hosts)
- `GET /api/hosts` - الحصول على جميع المضيفين (مع دعم pagination)
- `GET /api/hosts/:id` - الحصول على مضيف واحد
- `POST /api/hosts` - إضافة مضيف جديد
- `PUT /api/hosts/:id` - تحديث مضيف
- `DELETE /api/hosts/:id` - حذف مضيف
- `PATCH /api/hosts/:id/toggle-status` - تغيير حالة المضيف
- `POST /api/hosts/:id/check-status` - التحقق من حالة المضيف
- `GET /api/hosts/:id/history` - الحصول على تاريخ الحالات

### الوسوم (Tags)
- `GET /api/tags` - الحصول على جميع الوسوم
- `GET /api/tags/:id` - الحصول على وسم واحد
- `POST /api/tags` - إضافة وسم جديد
- `PUT /api/tags/:id` - تحديث وسم
- `DELETE /api/tags/:id` - حذف وسم

### مسح الشبكة
- `POST /api/network/scan` - مسح نطاق IP للعثور على المضيفين النشطين

### استيراد/تصدير
- `GET /api/export` - تصدير البيانات (JSON)
- `POST /api/import` - استيراد البيانات (JSON)

---

## 📖 الاستخدام

### إضافة مضيف جديد
1. اضغط على زر "إضافة مضيف جديد"
2. أدخل اسم المضيف وعنوان IP (مطلوب)
3. أضف وصف اختياري ورابط URL
4. اختر الوسوم (اختياري)
5. اضغط "إضافة المضيف"
6. سيتم التحقق تلقائياً من حالة المضيف

### عرض وإدارة المضيفين
1. ستظهر جميع المضيفين في جدول منظم
2. يمكنك البحث والفلترة حسب الحالة
3. يمكنك ترتيب الجدول حسب الاسم أو IP
4. يمكنك عرض/تعديل/حذف أي مضيف

### مسح الشبكة
1. اضغط على "مسح الشبكة"
2. أدخل نطاق الشبكة (مثال: `192.168.30.1-254` أو `192.168.30.0/24`)
3. اضغط "بدء المسح"
4. اختر المضيفين المراد إضافتهم
5. اضغط "إضافة المحددين"

### إدارة الوسوم
1. اضغط على "إدارة الوسوم"
2. أضف وسوم جديدة أو عدّل الموجودة
3. يمكنك ربط الوسوم بالمضيفين عند الإضافة أو التعديل

---

## 🛠️ التقنيات المستخدمة

### Frontend
- **React 18** - مكتبة واجهة المستخدم
- **Vite** - أداة البناء السريعة
- **React Router** - التوجيه بين الصفحات
- **Lucide React** - الأيقونات
- **React Toastify** - الإشعارات
- **Recharts** - الرسوم البيانية
- **CSS3** - التصميم مع Dark/Light Mode

### Backend
- **Node.js** - بيئة التشغيل
- **Express** - إطار عمل الخادم
- **SQLite (better-sqlite3)** - قاعدة البيانات
- **CORS** - السماح بالطلبات من مصادر مختلفة
- **ping** - التحقق من حالة المضيفين

---

## ⚡ تحسينات الأداء

تم تطبيق عدة تحسينات لزيادة أداء التطبيق:

- ✅ فهارس في قاعدة البيانات لتحسين سرعة الاستعلامات
- ✅ جلب الوسوم بشكل batch بدلاً من loop
- ✅ دعم pagination في API
- ✅ تحسين network scanner
- ✅ تحسين React components مع memoization
- ✅ تحسين استعلامات قاعدة البيانات

راجع ملف `PERFORMANCE_IMPROVEMENTS.md` للتفاصيل الكاملة.

---

## ⚠️ متطلبات DNS

**مهم**: للحصول على أسماء المضيفين (hostnames) عند مسح الشبكة، يجب أن يكون النظام (الخادم) يستخدم DNS server يحتوي على **PTR records** للأجهزة.

- البرنامج يستخدم DNS servers الافتراضية للنظام
- تأكد من أن DNS server في النظام يحتوي على PTR records للأجهزة
- إذا لم تظهر أسماء المضيفين، تحقق من إعدادات DNS في النظام

---

## 📝 ملاحظات إضافية

- قاعدة البيانات تُنشأ تلقائياً عند أول تشغيل للخادم
- البيانات محفوظة في ملف `server/network.db`
- يمكنك نسخ ملف `network.db` للنسخ الاحتياطي
- جميع الخطوط تستخدم خط **Tajawal** العربي الجميل
- التصميم متجاوب ويعمل على جميع الأجهزة

---

## 📄 الترخيص

راجع ملف `LICENSE` للتفاصيل.

---

**تاريخ التحديث:** 2024
