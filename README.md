# لوحة تحكم الشبكة 🌐

لوحة تحكم حديثة لإدارة ومتابعة الأجهزة في شبكتك مع قاعدة بيانات SQLite.

## المميزات

### إدارة الشبكات
- 📡 إدارة الشبكات (إضافة، تعديل، حذف)
- 🔍 فحص الشبكة لاكتشاف الأجهزة تلقائياً
- 📊 عرض بصري للشبكة (مربعات ملونة تمثل حالة كل IP)
- 📈 إحصائيات لكل شبكة (متصل، غير متصل، إجمالي)

### إدارة الأجهزة
- ➕ إضافة أجهزة جديدة (تلقائياً من خلال الفحص أو يدوياً)
- 📋 عرض الأجهزة في جدول منظم
- 🔍 البحث بالاسم أو IP
- 🔄 فلترة حسب حالة الاتصال (متصل/غير متصل)
- 🏷️ فلترة حسب الوسوم
- ⬆️⬇️ ترتيب تصاعدي/تنازلي على جميع الأعمدة
- ✏️ تعديل وسوم الأجهزة
- 🗑️ حذف الأجهزة (فردي أو جماعي)

### المتابعة والتحقق
- 💾 حفظ في قاعدة بيانات SQLite
- 🔄 التحقق من حالة الأجهزة (متصل/غير متصل)
- 📊 إحصائيات مفصلة (إجمالي، متصلة، غير متصلة)
- 📅 عرض تاريخ الإضافة وتاريخ آخر تحقق

### إدارة الوسوم
- 🏷️ إدارة الوسوم مع ألوان مخصصة
- 🔗 ربط الوسوم بالأجهزة
- 🎨 عرض الوسوم بشكل مرئي في الجداول

---

## 🚀 التثبيت والتشغيل

### المتطلبات

- Node.js (الإصدار 20 أو أحدث - موصى به: v22.x.x LTS)
- npm أو yarn
- نظام Linux أو Windows أو macOS

### الخطوات السريعة

#### 1️⃣ تثبيت مكتبات Frontend (الواجهة الأمامية)

افتح terminal وانتقل إلى مجلد المشروع:

```bash
cd /path/to/Shabakati
npm install
```

#### 2️⃣ تثبيت مكتبات Backend (الخادم)

في نفس terminal أو terminal جديد:

```bash
cd /path/to/Shabakati/server
npm install
```

**ملاحظة**: إذا واجهت مشاكل في تثبيت `better-sqlite3`، قد تحتاج إلى تثبيت أدوات البناء:

```bash
# Ubuntu/Debian
sudo apt-get install build-essential python3-dev

# macOS
xcode-select --install

# Fedora/RHEL
sudo dnf groupinstall "Development Tools"
sudo dnf install python3-devel
```

---

## 💻 التشغيل في وضع التطوير (Development)

### الخطوات:

#### Terminal 1 - تشغيل Backend Server

```bash
cd /path/to/Shabakati/server
npm run dev
```

✅ **الخادم سيعمل على:** `http://0.0.0.0:3001` (متاح من جميع الواجهات)

**⚠️ اترك هذا Terminal مفتوحاً!**

#### Terminal 2 - تشغيل Frontend

افتح **terminal جديد** (لا تغلق terminal الخادم):

```bash
cd /path/to/Shabakati
npm run dev
```

✅ **الواجهة ستعمل على:** `http://0.0.0.0:5173` (متاحة من جميع الواجهات)

#### فتح البرنامج

**من نفس السيرفر:**
افتح المتصفح واذهب إلى:
```
http://localhost:5173
```

**من أي جهاز في الشبكة المحلية:**
افتح المتصفح واذهب إلى:
```
http://<SERVER_IP>:5173
```
حيث `<SERVER_IP>` هو عنوان IP للسيرفر في الشبكة (مثال: `http://192.168.1.100:5173`)

**ملاحظة:** تأكد من أن جدار الحماية (firewall) يسمح بالاتصال على المنافذ 3001 و 5173.

---

## 🚀 التشغيل في Production

### الخطوة 1: بناء Frontend

```bash
cd /path/to/Shabakati
npm run build
```

سيتم إنشاء مجلد `dist/` يحتوي على ملفات الإنتاج.

### الخطوة 2: إعداد Backend

#### الخيار أ: استخدام Node.js مباشرة

```bash
cd /path/to/Shabakati/server
npm start
```

#### الخيار ب: استخدام PM2 (موصى به)

تثبيت PM2:
```bash
npm install -g pm2
```

تشغيل Backend مع PM2:
```bash
cd /path/to/Shabakati/server
pm2 start server.js --name network-dashboard-api
pm2 save
pm2 startup
```

**إدارة PM2:**
```bash
pm2 list              # عرض جميع العمليات
pm2 logs              # عرض السجلات
pm2 restart network-dashboard-api  # إعادة التشغيل
pm2 stop network-dashboard-api     # إيقاف
pm2 delete network-dashboard-api   # حذف
```

### الخطوة 3: إعداد Reverse Proxy (Nginx)

إنشاء ملف إعداد Nginx `/etc/nginx/sites-available/network-dashboard`:

```nginx
server {
    listen 80;
    server_name your-domain.com;  # استبدل بدومينك

    # Frontend - ملفات ثابتة
    location / {
        root /path/to/Shabakati/dist;
        try_files $uri $uri/ /index.html;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

تفعيل الإعداد:
```bash
sudo ln -s /etc/nginx/sites-available/network-dashboard /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### الخطوة 4: إعداد SSL (HTTPS) - موصى به

استخدام Certbot:
```bash
sudo apt-get install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

### الخطوة 5: متغيرات البيئة (اختياري)

يمكنك إنشاء ملف `.env` في مجلد `server/`:

```env
PORT=3001
NODE_ENV=production
```

وتعديل `server.js` لقراءة هذه المتغيرات.

---

## 📁 الواجهة والصفحات

### الصفحة الرئيسية (Dashboard)
- عرض إحصائيات عامة (عدد الشبكات، عدد الأجهزة، أجهزة متصلة/غير متصلة)
- قائمة بالشبكات مع إحصائيات كل شبكة
- زر لحذف جميع البيانات

### إدارة الشبكات
- عرض قائمة بجميع الشبكات
- إضافة شبكة جديدة (الاسم، Network ID، Subnet)
- تعديل شبكة موجودة
- حذف شبكة (مع حذف جميع أجهزتها)
- عرض تاريخ آخر فحص

### عرض الشبكة
- عرض بصري للشبكة (مربعات ملونة تمثل كل IP)
  - 🟢 أخضر: جهاز متصل
  - 🔴 أحمر: جهاز غير متصل
  - ⚪ أبيض: لا يوجد جهاز
- جدول بجميع الأجهزة في الشبكة
- فحص الشبكة لاكتشاف الأجهزة
- حذف جميع أجهزة الشبكة
- فلترة الأجهزة (حسب الحالة، الوسم، البحث)
- ترتيب الأجهزة (الاسم، IP، الحالة، آخر فحص)
- تعديل وسوم كل جهاز

### إدارة الوسوم
- عرض قائمة بجميع الوسوم
- إضافة وسم جديد (مع لون مخصص)
- تعديل وسم موجود
- حذف وسم

---

## 📁 هيكل المشروع

```
Shabakati/
├── src/                    # كود الواجهة الأمامية (React)
│   ├── components/         # المكونات المشتركة
│   │   └── ErrorBoundary.jsx
│   ├── pages/              # الصفحات الرئيسية
│   │   ├── HostsList.jsx   # الصفحة الرئيسية - Dashboard
│   │   ├── NetworksList.jsx # إدارة الشبكات
│   │   ├── NetworkView.jsx # عرض الشبكة
│   │   └── TagsManagement.jsx # إدارة الوسوم
│   ├── utils/              # دوال مساعدة
│   │   ├── api.js          # دوال API
│   │   └── networkUtils.js # دوال حساب الشبكة
│   ├── constants.js        # الثوابت
│   ├── App.jsx
│   └── main.jsx
├── server/                 # كود الخادم (Node.js)
│   ├── server.js           # ملف الخادم الرئيسي
│   ├── database.js         # إدارة قاعدة البيانات
│   ├── networkScanner.js   # مسح الشبكة
│   ├── networkUtils.js     # دوال حساب الشبكة
│   ├── package.json
│   └── network.db          # ملف قاعدة البيانات (يُنشأ تلقائياً)
├── dist/                   # ملفات البناء (بعد npm run build)
├── public/                 # الملفات الثابتة
│   └── favicon.svg
├── package.json            # مكتبات الواجهة
├── vite.config.js          # إعدادات Vite
└── README.md
```

---

## 💾 قاعدة البيانات

- **النوع**: SQLite
- **الموقع**: `server/network.db`
- **الجداول**:
  - `networks` - الشبكات
  - `hosts` - الأجهزة
  - `tags` - الوسوم
  - `host_tags` - ربط الوسوم بالأجهزة
  - `host_status_history` - تاريخ حالات الأجهزة

### جدول networks:
- `id` (INTEGER PRIMARY KEY)
- `name` (TEXT) - اسم الشبكة
- `network_id` (TEXT) - Network ID (مثل: 192.168.1.0)
- `subnet` (INTEGER) - Subnet mask (مثل: 24)
- `created_at` (TEXT) - تاريخ الإنشاء
- `last_scanned` (TEXT) - تاريخ آخر فحص

### جدول hosts:
- `id` (INTEGER PRIMARY KEY)
- `name` (TEXT) - اسم الجهاز
- `ip` (TEXT) - عنوان IP
- `network_id` (INTEGER) - ID الشبكة (FOREIGN KEY)
- `description` (TEXT) - الوصف
- `url` (TEXT) - رابط URL
- `status` (TEXT) - الحالة (online/offline)
- `created_at` (TEXT) - تاريخ الإضافة
- `last_checked` (TEXT) - تاريخ آخر تحقق
- `ping_latency` (REAL) - زمن الاستجابة
- `packet_loss` (REAL) - فقدان الحزم
- `uptime_percentage` (REAL) - نسبة الوقت المتصل

### الفهارس (Indexes):
تم إضافة فهارس لتحسين الأداء:
- `idx_hosts_ip` - على عمود IP
- `idx_hosts_status` - على عمود status
- `idx_hosts_network_id` - على network_id
- `idx_host_tags_host_id` - على host_id في host_tags
- `idx_host_tags_tag_id` - على tag_id في host_tags
- `idx_status_history_host_id` - على host_id في host_status_history

### نسخ احتياطي:

لإنشاء نسخة احتياطية من قاعدة البيانات:
```bash
cp server/network.db server/network.db.backup
```

لاستعادة النسخة الاحتياطية:
```bash
cp server/network.db.backup server/network.db
```

---

## 🔌 API Endpoints

### الشبكات (Networks)
- `GET /api/networks` - الحصول على جميع الشبكات
- `GET /api/networks/:id` - الحصول على شبكة واحدة
- `POST /api/networks` - إضافة شبكة جديدة
- `PUT /api/networks/:id` - تحديث شبكة
- `DELETE /api/networks/:id` - حذف شبكة (مع حذف جميع أجهزتها)
- `GET /api/networks/:id/hosts` - الحصول على أجهزة الشبكة
- `POST /api/networks/:id/scan` - فحص الشبكة لاكتشاف الأجهزة
- `DELETE /api/networks/:id/hosts` - حذف جميع أجهزة الشبكة

### الأجهزة (Hosts)
- `GET /api/hosts` - الحصول على جميع الأجهزة
- `GET /api/hosts/:id` - الحصول على جهاز واحد
- `POST /api/hosts` - إضافة جهاز جديد
- `PUT /api/hosts/:id` - تحديث جهاز
- `DELETE /api/hosts/:id` - حذف جهاز
- `POST /api/hosts/:id/check-status` - التحقق من حالة الجهاز
- `GET /api/hosts/:id/history` - الحصول على تاريخ الحالات

### الوسوم (Tags)
- `GET /api/tags` - الحصول على جميع الوسوم
- `GET /api/tags/:id` - الحصول على وسم واحد
- `POST /api/tags` - إضافة وسم جديد
- `PUT /api/tags/:id` - تحديث وسم
- `DELETE /api/tags/:id` - حذف وسم

### الإحصائيات والإدارة
- `GET /api/stats` - الحصول على الإحصائيات العامة
- `DELETE /api/data/all` - حذف جميع البيانات (أجهزة، شبكات، وسوم)

### استيراد/تصدير
- `GET /api/export` - تصدير البيانات (JSON)
- `POST /api/import` - استيراد البيانات (JSON)

---

## 📖 دليل الاستخدام

### إضافة شبكة جديدة
1. اذهب إلى صفحة "إدارة الشبكات"
2. اضغط على "إضافة شبكة جديدة"
3. أدخل:
   - اسم الشبكة (مثل: "الشبكة الرئيسية")
   - Network ID (مثل: 192.168.1.0)
   - Subnet (مثل: 24)
4. اضغط "إضافة"

### فحص الشبكة
1. اذهب إلى صفحة عرض الشبكة
2. اضغط على "فحص الشبكة"
3. انتظر حتى يكتمل الفحص
4. سيتم اكتشاف الأجهزة تلقائياً وإضافتها

### إدارة الأجهزة في الشبكة
- **الفلترة**: استخدم حقول البحث والفلترة في أعلى الجدول
- **الترتيب**: اضغط على رأس العمود للترتيب
- **تعديل الوسوم**: اضغط "تعديل" بجانب الجهاز
- **حذف**: اضغط "حذف" لإزالة الجهاز

### إدارة الوسوم
1. اذهب إلى "إدارة الوسوم"
2. اضغط "إضافة وسم جديد"
3. أدخل اسم الوسم واختر لون
4. اضغط "إضافة"

---

## 🛠️ التقنيات المستخدمة

### Frontend
- **React 18** - مكتبة واجهة المستخدم
- **Vite** - أداة البناء السريعة
- **React Router** - التوجيه بين الصفحات
- **CSS3** - التصميم
- **Tajawal Font** - خط عربي من Google Fonts

### Backend
- **Node.js** - بيئة التشغيل
- **Express** - إطار عمل الخادم
- **SQLite (better-sqlite3)** - قاعدة البيانات
- **CORS** - السماح بالطلبات من مصادر مختلفة
- **ping** - التحقق من حالة الأجهزة (ICMP)
- **net** - مسح المنافذ (Port Scanning)

---

## ⚡ تحسينات الأداء

### قاعدة البيانات
- ✅ فهارس على الأعمدة المستخدمة بكثرة
- ✅ جلب الوسوم بشكل batch بدلاً من loop
- ✅ استعلامات محسّنة

### Network Scanner
- ✅ فحص ICMP (ping) والمنافذ بالتوازي
- ✅ دعم 26 منفذ شائع (Linux، Windows، خدمات مشهورة)
- ✅ تحسين timeout للمسح الأسرع

### React Frontend
- ✅ استخدام `useCallback` و `useMemo` لتقليل إعادة الـ render
- ✅ استخدام `Intl.Collator` للترتيب السريع للنصوص العربية
- ✅ Lazy loading للصفحات

---

## 🔧 حل المشاكل الشائعة

### خطأ: "Port 3001 already in use"
**الحل:** 
- أغلق البرنامج الذي يستخدم المنفذ 3001
- أو غير المنفذ في `server/server.js`

### خطأ: "Cannot find module"
**الحل:** 
- شغل `npm install` في المجلد الذي يظهر فيه الخطأ
- تأكد من تثبيت جميع المكتبات في كلا المجلدين

### خطأ: "EADDRINUSE" على المنفذ 5173
**الحل:** 
- أغلق terminal الواجهة وأعد تشغيله
- أو غير المنفذ في `vite.config.js`

### خطأ: "Network request failed"
**الحل:** 
- تأكد من أن الخادم يعمل على `http://localhost:3001`
- تأكد من تشغيل الخادم قبل الواجهة

### خطأ في تثبيت `better-sqlite3`
**الحل:** 
- تأكد من تثبيت أدوات البناء (build-essential، python3-dev)
- راجع قسم "المتطلبات" أعلاه

### لا تظهر الأجهزة بعد الفحص
**الحل:** 
- تأكد من أن الفحص اكتمل (انتظر رسالة النجاح)
- تأكد من أن الأجهزة في نفس الشبكة
- جرب فحص IP محدد أولاً

### المربعات لا تظهر ملونة
**الحل:** 
- تأكد من أن subnet هو /24 أو أكبر
- الشبكات الكبيرة (/16) قد لا تعرض المربعات

---

## 📝 ملاحظات إضافية

- قاعدة البيانات تُنشأ تلقائياً عند أول تشغيل للخادم
- البيانات محفوظة في ملف `server/network.db`
- يمكنك نسخ ملف `network.db` للنسخ الاحتياطي
- جميع الخطوط تستخدم خط **Tajawal** العربي من Google Fonts
- البرنامج يدعم RTL (من اليمين لليسار) بشكل كامل
- للتشغيل في Production، استخدم PM2 و Nginx كما هو موضح أعلاه

---

## 📄 الترخيص

راجع ملف `LICENSE` للتفاصيل.

---

**تاريخ التحديث:** 2024
